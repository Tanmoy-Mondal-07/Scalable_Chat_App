import cassandra from "cassandra-driver";

// // Generate a *random* UUID (v4)
// const randomUuid = cassandra.types.Uuid.random();

// // Generate a *time-based* UUID (v1)
// const timeUuid = cassandra.types.TimeUuid.now();

// console.log("Random UUID:", randomUuid.toString());
// console.log("Time UUID:", timeUuid.toString());